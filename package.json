{
  "name": "smart-job-graph",
  "version": "1.0.0",
  "description": "Graph database job recommendation application for the Wexa AI take-home assignment.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "seed": "node scripts/seed.js",
    "check-db": "node scripts/check-db.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^17.2.0",
    "express": "^5.1.0",
    "neo4j-driver": "^6.0.0"
  }
}
